```
播放逻辑说明
有 music
判断是否存在于播放列表
  不存在 -> 添加
  存在
判断是否等于 current
  等于
    是否播放中
      是 -> 暂停
      否 -> 开始
  不等于
    设置为 current 并播放
无 music
判断 current
  有
    是否播放中
      是 -> 暂停
      否 -> 开始
  无
    判断播放列表是否为空
      不为空 => 选取播放列表中的第一个设置为 current 播放
      为空 -> 提示
```
